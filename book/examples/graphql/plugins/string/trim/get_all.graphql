query GetAllTrim {
  instances {
    entities(
      type: {
        namespace: "string"
        name: "trim"
      }
    ) {
      id
      properties {
        name
        value
      }
    }
  }
}
