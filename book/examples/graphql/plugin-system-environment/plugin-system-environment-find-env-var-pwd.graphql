query findSystemEnvHome {
  instances {
    entities(label: "/org/inexor/system/env/pwd") {
      id
      label
      type {
        name
      }
      properties(names: ["name", "value"]) {
        name
        value
      }
    }
  }
}
