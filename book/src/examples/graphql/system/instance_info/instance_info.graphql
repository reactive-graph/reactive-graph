query GetInstanceInfo {
  system {
    instanceInfo {
      hostname
      port
      secure

      name
      description

      version
      pluginApiVersion
      rustcVersion

      gitBranch
      gitCommit
      buildDate

      lastSeen
    }
  }
}
