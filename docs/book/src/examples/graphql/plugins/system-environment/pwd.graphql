query findSystemEnvHome {
  instances {
    entities(label: "/io/reactive-graph/system/env/pwd") {
      id
      label
      type {
        name
      }
      properties(names: ["name", "value"]) {
        name
        value
      }
    }
  }
}
