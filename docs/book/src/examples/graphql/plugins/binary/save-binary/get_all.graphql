query GetAllSaveBinary {
  instances {
    entities(
      type: {
        namespace: "binary"
        name: "save_binary"
      }
    ) {
      id
      label
      description
      properties {
        name
        value
      }
    }
  }
}
