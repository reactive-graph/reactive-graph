query {
  types {
    components(name: "component1") {
      name
      description
      properties {
        name
        dataType
        socketType
        extensions {
          name
          extension
        }
      }
      # extensions ...
    }
  }
}
