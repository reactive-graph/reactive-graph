query {
    types {
        entities(
            type: {
                namespace: "book"
                name: "example"
            }
        ) {
            namespace
            name
            outboundRelations {
                namespace
                name
            }
        }
    }
}
