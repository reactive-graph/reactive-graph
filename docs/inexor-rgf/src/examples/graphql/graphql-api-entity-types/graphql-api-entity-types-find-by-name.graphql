query {
  types {
    entities(search: "*xamp*") {
      name
      description
      components {
        name
      }
      properties {
        name
        dataType
        socketType
      }
    }
  }
}
