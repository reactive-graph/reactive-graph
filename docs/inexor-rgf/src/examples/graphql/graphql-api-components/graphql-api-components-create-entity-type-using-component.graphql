mutation {
  types {
    entities {
      create(
        namespace: "book",
        name: "example1",
        components: [ "component1" ]
      ) {
        ...FullEntityType
      }
    }
  }
}

fragment FullEntityType on EntityType {
  name
  description
  properties {
    name
    dataType
    socketType
  }
}
