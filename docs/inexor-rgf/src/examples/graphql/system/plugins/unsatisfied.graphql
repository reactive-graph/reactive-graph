query UnsatisfiedDependenciesOfAPlugin {
  system {
    plugins(hasUnsatisfiedDependencies: true) {
      name
      state
      unsatisfiedDependencies {
        name
        state
      }
    }
  }
}
