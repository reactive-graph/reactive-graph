mutation FetchRemotesFromAllRemotes {
  system {
    remotes {
      fetchRemotesFromAllRemotes {
        hostname
        port
        secure

        name
        description

        version
        pluginApiVersion
        rustcVersion

        gitBranch
        gitCommit
        buildDate

        lastSeen
      }
    }
  }
}
