query GetAllConfigs {
  instances {
    entities(
      type: {
        namespace: "config"
        name: "config_file"
      }
    ) {
      id
      label
      description
      properties {
        name
        value
      }
    }
  }
}
