query GetAllRemotes {
  system {
    remotes {
      buildDate
      description
      gitBranch
      gitCommit
      hostname
      lastSeen
      name
      pluginApiVersion
      port
      rustcVersion
      secure
      version
    }
  }
}
