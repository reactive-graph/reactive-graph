mutation FetchRemotesFromAllRemotes {
  remotes {
    fetchRemotesFromAllRemotes {
      hostname
      port
      secure

      name
      description

      version
      pluginApiVersion
      rustcVersion

      gitBranch
      gitCommit
      buildDate

      lastSeen
    }
  }
}
