mutation FetchRemotesFromRemote($address: InstanceAddress!) {
  remotes {
    fetchRemotesFromRemote(address: $address) {
      hostname
      port
      secure

      name
      description

      version
      pluginApiVersion
      rustcVersion

      gitBranch
      gitCommit
      buildDate

      lastSeen
    }
  }
}
