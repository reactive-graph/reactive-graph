mutation AddRemote($address: InstanceAddress!) {
  remotes {
    add(address: $address) {
      buildDate
      description
      gitBranch
      gitCommit
      hostname
      lastSeen
      name
      pluginApiVersion
      port
      rustcVersion
      secure
      version
    }
  }
}
