query listRelationBehaviourInstances {
  behaviours {
    relations {
      namespace
      name
      instances {
        outbound {id}
        type { name }
        instanceId
        inbound {id}
      }
    }
  }
}
