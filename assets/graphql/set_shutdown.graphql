mutation {
  instances {
    entities {
      update(
        label: "/org/inexor/system/shutdown",
        properties: [
          {
            name: "shutdown",
            value: true
          }
        ]
      ) {
        id,
        type {name},
        properties(
          names: [
            "shutdown",
          ]
        )
      }
    }
  }
}
