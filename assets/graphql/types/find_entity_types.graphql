query {
  types {
    entities(search: "entitytype?") {
      namespace
      name
      description
      components {
        name
      }
      properties {
        name
        dataType
        socketType
      }
    }
  }
}
