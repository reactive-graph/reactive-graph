query {
  types {
    components(
      type: {
        namespace: "logical"
        name: "action"
      }
    ) {
      namespace
      name
      description
      properties {
        name
        dataType
        socketType
        extensions {
          name
          extension
        }
      }
    }
  }
}
