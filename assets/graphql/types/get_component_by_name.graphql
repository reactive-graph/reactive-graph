query {
  types {
    components(name: "component1") {
      namespace
      name
      description
      properties {
        name
        dataType
        socketType
        extensions {
          name
          extension
        }
      }
    }
  }
}
