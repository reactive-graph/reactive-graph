query {
  types {
    components(namespace: "example", name: "component1") {
      namespace
      name
      description
      properties {
        name
        dataType
        socketType
        extensions {
          name
          extension
        }
      }
    }
  }
}
