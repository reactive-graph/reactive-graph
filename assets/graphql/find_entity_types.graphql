query {
  types {
    entities(search: "entitytype?") {
      name
      description
      components {
        name
      }
      properties {
        name
        dataType
        socketType
      }
    }
  }
}
