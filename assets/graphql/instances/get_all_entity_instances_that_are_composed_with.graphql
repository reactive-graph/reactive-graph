query getAllEntityInstancesThatAreComposedWith {
  instances {
    composedWith: entities(components: "labeled") {
      id
      label
      components
      behaviours
      properties(name: "label") {
        name
        value
      }
      behaviours
    }
  }
}
