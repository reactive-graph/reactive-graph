query getFlowByLabel {
  instances {
    flows(label: "/org/qualityland/mqtt") {
      id
      label
      wrapper {
        properties {
          name
          value
        }
      }
    }
  }
}
