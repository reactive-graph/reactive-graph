query {
  types {
    entities(name: "entitytype1") {
      name
      description
      components {
        name
      }
      properties {
        name
        dataType
        socketType
      }
    }
  }
}
