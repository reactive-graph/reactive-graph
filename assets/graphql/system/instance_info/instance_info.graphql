query InstanceInfo {
  system {
    instanceInfo {
      name
      description
      hostname
      port
      secure
      version
      buildDate
      gitBranch
      gitCommit
      rustcVersion
      pluginApiVersion
    }
  }
}
